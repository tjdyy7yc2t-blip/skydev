{
  "name": "skydev-website",
  "version": "1.0.0",
  "description": "SkyDev Web Services - Sitio oficial",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "gh-pages": "^6.1.1"
  },
  "dependencies": {
    "aos": "^2.3.4",
    "swiper": "^11.1.0"
  }
}
